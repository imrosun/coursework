/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack(config) {
    //     // Exclude LICENSE files or other unwanted files
    //     config.module.rules.push({
    //       test: /\.txt$/,
    //       use: 'ignore-loader'
    //     });
    
    //     return config;
    //   }
};

export default nextConfig;


